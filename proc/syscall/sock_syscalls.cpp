#include <proc/syscalls.hpp>
#include <global.hpp>

using namespace cloudos;

cloudabi_errno_t cloudos::syscall_sock_accept(syscall_context &)
{
	return ENOSYS;
}

cloudabi_errno_t cloudos::syscall_sock_bind(syscall_context &)
{
	return ENOSYS;
}

cloudabi_errno_t cloudos::syscall_sock_connect(syscall_context &)
{
	return ENOSYS;
}

cloudabi_errno_t cloudos::syscall_sock_listen(syscall_context &)
{
	return ENOSYS;
}

cloudabi_errno_t cloudos::syscall_sock_recv(syscall_context &)
{
	return ENOSYS;
}

cloudabi_errno_t cloudos::syscall_sock_send(syscall_context &)
{
	return ENOSYS;
}

cloudabi_errno_t cloudos::syscall_sock_shutdown(syscall_context &)
{
	return ENOSYS;
}

cloudabi_errno_t cloudos::syscall_sock_stat_get(syscall_context &)
{
	return ENOSYS;
}

